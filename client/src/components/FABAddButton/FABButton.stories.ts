import { Meta } from "@storybook/react";
import FABAddButton from ".";

const meta: Meta = {
    title: "Components/FABAddButton",
    component: FABAddButton,
}

export default meta;

export const Primary = {}